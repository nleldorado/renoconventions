import React from 'react';
import Layout from '../components/layout'

const contact = () => {
    return (
        <div>
            <Layout>
            </Layout>
        </div>
    );
};

export default contact;